/// winit window lifecycle stub.
/// Full impl: create EventLoop, Window, wgpu Surface, then hand off to FrameRenderer.
pub struct TerminalWindow;

impl TerminalWindow {
    pub fn run() {
        // winit::event_loop::EventLoop::new()
        //   .run(|event, target| { ... })
        todo!("window event loop")
    }
}
