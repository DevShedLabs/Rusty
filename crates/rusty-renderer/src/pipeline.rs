/// wgpu render pipeline stub.
/// Full implementation: create shader modules, bind group layouts,
/// vertex/fragment stages, and the pipeline descriptor.
pub struct TerminalPipeline {
    // pub inner: wgpu::RenderPipeline,
}

impl TerminalPipeline {
    pub fn new(/* device: &wgpu::Device, format: wgpu::TextureFormat */) -> Self {
        Self {}
    }
}
